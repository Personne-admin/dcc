=== FILE: main.dc ===
module m;

struct Pair {
    i32 x;
    f64 y;
}

i32 swapped_shorthand() {
    f64 y = 2.0;
    Pair p = {y, 1};
    return p.x;
}

=== EXPECT-ERRORS ===
main.dc:10: identifier `y` matches field `y` but appears in a positional brace literal
