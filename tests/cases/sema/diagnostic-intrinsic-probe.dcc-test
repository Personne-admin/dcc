=== FILE: main.dc ===
module test;
import core;
void compile_error([]const u8 message) {}
public void test() {
    // Ordinary user functions with the same name are not intrinsics.
    compile_error("ordinary call");
    static if compiles { core::compile_error("failed requirement"); } {
        core::compile_error("probe unexpectedly succeeded");
    }
    static match 1 {
        1 => {},
        _ => { core::compile_error("discarded match arm"); unknown(); },
    }
}

=== EXPECT-ERROR-COUNT: 0 ===
